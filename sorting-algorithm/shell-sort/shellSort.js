/**
 *
 * TC for Shell short is nlogn => where n is the scanning the list and logn is the number of passes.
 * It can also be O(N^3/2) or O(N^5/3) when the gap is taken as prime number
 */

function shellSort(arr) {
  let gap, i, j;

  for (gap = Math.floor(arr.length / 2); gap > 1; gap = gap / 2) {

    for(i= gap; i)
  }
}
